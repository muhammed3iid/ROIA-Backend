package com.ROIA.shared_unit.Repository;

import com.ROIA.shared_unit.Model.SharedUnit;
import org.springframework.data.jpa.repository.JpaRepository;


public interface SharedUnitRepository extends JpaRepository<SharedUnit, Long> {
}
